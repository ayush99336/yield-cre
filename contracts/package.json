{
  "name": "@yield-cre/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "forge script script/Deploy.s.sol --rpc-url ${ETH_SEPOLIA_RPC_URL:-http://localhost:8545} --broadcast --private-key ${DEPLOYER_PRIVATE_KEY:-0x00}",
    "build": "forge build",
    "lint": "forge fmt --check",
    "test": "forge test"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.5.0",
    "@openzeppelin/contracts": "^5.6.1"
  }
}
