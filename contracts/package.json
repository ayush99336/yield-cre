{
  "name": "@yield-cre/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Contracts scaffold ready. Add Foundry or Hardhat next.'",
    "build": "echo 'Build not configured yet'",
    "lint": "echo 'Lint not configured yet'",
    "test": "echo 'Tests not configured yet'"
  }
}
